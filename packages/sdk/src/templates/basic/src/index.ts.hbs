// {{displayName}} - Módulo para Plataforma.app
// Gerado automaticamente pelo Plataforma SDK

import React from 'react';
import { ModuleComponent } from './components/ModuleComponent';
import type { ModuleConfig, ModuleExports } from './types';

// Configuração do módulo
export const config: ModuleConfig = {
  name: '{{name}}',
  displayName: '{{displayName}}',
  version: '{{version}}',
  category: '{{category}}',
  description: '{{description}}',
  icon: 'extension',
  color: '#3B82F6'
};

// Componente principal do módulo
export const Component = ModuleComponent;

// Exportações do módulo
const moduleExports: ModuleExports = {
  config,
  Component,
  
  // Hooks personalizados (se houver)
  hooks: {},
  
  // Utilitários (se houver)
  utils: {},
  
  // Serviços (se houver)
  services: {}
};

export default moduleExports;

// Exportações nomeadas para conveniência
export * from './components';
export * from './types';
export * from './utils';